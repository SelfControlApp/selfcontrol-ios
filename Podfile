# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SCFilterControlProvider' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SCFilterControlProvider

end

target 'SCFilterDataProvider' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SCFilterDataProvider

end

target 'SelfControlIOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SelfControlIOS
  pod 'Masonry', '~> 1.0.2'

  target 'SelfControlIOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SelfControlIOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
